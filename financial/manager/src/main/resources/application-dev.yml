spring:
  jackson:
    date-format: 'yyyy-MM-dd HH:mm:ss'
    time-zone: GMT+8
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/foo?useUnicode=true
    username: root
    password: root
  jpa:
    show-sql: true
server:
  context-path: /manager